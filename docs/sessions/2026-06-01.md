# 세션: 2026-06-01

## 현재 상태 (이 시점의 스냅샷)

| 컴포넌트 | 상태 | 비고 |
|---------|------|------|
| 스크래퍼 | 작동 중 | 마감일 상대값 버그 존재 |
| 분석 Agent | 완료 | `.claude/agents/rfp-analyzer.md` |
| 대시보드 | 완료 + 검증됨 | `dashboard/generate.py` + `dashboard/index.html` |

## 오늘 결정한 것

### 1. rfp-analyzer Agent 설계 확정
- **모델**: `sonnet`, 인터랙션 없음, Markdown 출력
- **사업 분야 분류 추가**: IT/SW개발 | 데이터·AI | 인프라·HW | 정보보안 | 컨설팅·기획 | 기타
- **파일 위치**: `.claude/agents/rfp-analyzer.md`

### 2. analyze.py 보류 결정
- Claude API 별도 과금 없이 구독만 사용 방침
- **수동 워크플로우**: 수집 문서 → rfp-analyzer 직접 호출 → result.md 저장

### 3. 메인 세션 역할 명확화 (CLAUDE.md 업데이트)
- 메인 세션: 조언·분석 + `docs/sessions/` 기록만
- 파일 작업 전부 → 해당 agent 위임 (필수)

### 4. 대시보드 설계 및 생성 완료
- **표시 항목**: 공고명, 발주처, 분야, 예산, 마감일, 분석여부, Executive Summary
- **데이터 흐름**: Excel + result.md → generate.py → data.json → index.html

### 5. End-to-End 테스트 완료 (NIPA 대상)
- 스크래퍼 정상 동작: 공고 8건, 첨부파일 17개
- rfp-analyzer 정상 동작: result.md 생성 확인
- generate.py 버그 4건 수정 후 data.json 정상 생성
- 대시보드 기능 코드 검증 완료

### 6. output/analysis/ 폴더 구조 확정
```
output/analysis/{사이트}-{nttNo}/
├── meta.json
└── result.md
```

## 발견된 기술 부채 (추후 수정 필요)

| 문제 | 영향 | 우선순위 |
|------|------|---------|
| NIPA 마감일이 "D-3" 상대값으로 수집됨 | 대시보드 마감 임박 강조 미동작 | 높음 |
| Excel 공고번호 컬럼이 D-day 값 | ID 추적 불안정 | 중간 |
| 중복 공고 수집 (같은 공고 2회) | data.json에 중복 항목 | 중간 |

## 다음에 해야 할 것

- [ ] 브라우저에서 대시보드 직접 확인 (`python -m http.server 8000`)
- [ ] NIPA 스크래퍼 마감일 절대값 수집으로 수정 (coding agent 위임)
- [ ] 중복 공고 수집 방지 로직 점검

## 프로토타입 성공 기준 달성 여부

| 기준 | 결과 |
|------|------|
| 특정 사이트 1곳에서 문서 1개 안정적 저장 | ✅ NIPA 공고 + 첨부파일 |
| AI가 해당 문서 요약 1개 출력 | ✅ result.md 생성 |
| 결과가 브라우저 HTML 화면에 표시 | ✅ (서버 실행 후 확인 단계) |

---

## 재개 프롬프트

```
docs/sessions/2026-06-01.md 파일을 읽고 이전 논의를 파악한 후,
"NIPA 마감일 절대값 수집 버그 수정"부터 이어서 진행해줘.
```
