> 참고자료 폴더 — 이 폴더는 개발 참고·컨텍스트 자료 보관소입니다. 프로젝트 정본이 아닙니다.
> 동작을 바꾸려면 정본을 수정하세요. 이 폴더의 파일을 수정해도 시스템에 반영되지 않습니다.

# docs/refference/ — 개발 참고자료

## 폴더 역할

설계·프롬프트 설계 과정에서 생성된 참고 기록을 보관합니다.
에이전트·스크래퍼·대시보드의 실제 동작은 아래 **정본 경로**에 있습니다.

> **주의**: `RFP_Template.html` 은 예외적으로 에이전트가 런타임에 직접 읽는 LIVE 파일입니다 (아래 표 참조).

---

## 파일 목록

| 파일 | 설명 | 성격 | 대응 정본 경로 |
|---|---|---|---|
| `RFP_Analyzer_Prompt_v3.md` | rfp-analyzer 프롬프트 설계 초안 v3 | 참고 (정본 아님) | `.claude/agents/rfp-analyzer.md` |
| `RFP_Template.html` | 분석 리포트 HTML 템플릿 | **LIVE** — 에이전트가 런타임에 읽음 | 이 파일 자체가 정본 |

---

## LIVE 파일 주의

`RFP_Template.html` 은 rfp-analyzer 에이전트가 분석 리포트 생성 시 이 경로를 직접 Read합니다.

- 삭제·이동·폴더명 변경 금지
- 이동이 필요한 경우 `.claude/agents/rfp-analyzer.md` 의 경로 참조도 함께 수정할 것

---

## 폴더명 오타 안내

`refference` 는 `reference` 의 오타입니다.
`RFP_Template.html` 을 에이전트가 이 경로로 참조하므로 폴더명 변경은 코드 수정을 유발합니다.
이름 수정은 다음 개발 단계로 분리 보류 중입니다.
